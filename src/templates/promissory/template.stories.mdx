import { Meta, Preview, Props, Description, Story } from "@storybook/addon-docs/blocks";
import { object } from "@storybook/addon-knobs";
import { PromissoryTemplate } from "./template";
import { promissoryTemplate } from "../samples/promissoryTemplate";

<Meta title="MDX|PromissoryTemplate" component={PromissoryTemplate} />

# PhytoTemplate component

<Description of={PromissoryTemplate} />

# Props

<Props of={PromissoryTemplate} />

# Usage

<Preview>
  <Story name="basic sample">
    <PromissoryTemplate document={object("document", promissoryTemplate)} />
  </Story>
</Preview>